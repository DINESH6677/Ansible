- name: install and start nginx
  hosts: Frontend
  become: yes
  tasks:
  - name: Install nginx
    ansible.builtin.package:
      name: nginx
      state: install

  - name: Start service nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
    